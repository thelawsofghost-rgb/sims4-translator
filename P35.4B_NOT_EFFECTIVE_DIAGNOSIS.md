# P35.4-B — 「override 未生效」只读定位方案 v2

现象（用户回报）：游戏可启动、WW 动画列表正常、动画存在，但 display_name 仍显示英文 raw → **override 包被放进 Mods 但未被消费**。

约束（铁律，全程不变）：不重译 / 不改源 WW 包 / 不生成 STBL。/ 本方案全部为**只读取证**。

当前部署：`P35.1_ww_anim_displayname_override.package`（已放本地 Mods）。

---

## 0. 先说清两个已证事实（避免走弯路）

**事实 1 — override 的 TGI 是源包的忠实影子。**
P35.1 builder 把源 WW_ANIM_XML 的 `group_id`、`instance_id`(含 flags 位)、`type_id` 以及 meta **原样照抄**进 override（`build_package([(t_type,t_group,t_inst,new_body,meta)])`）。
→ 若 WW 是经 Sim 资源管理器按**这一个 TGI** 去 load，则该 loose `.package` 按 Sims 常规 override 语义**理应遮蔽**源（这是 XML override 的标准行为）。

**事实 2 — 显示文本有「两条路」，UI 显示哪条决定成败（来自此前 display flow 反汇编）。**
`SexAnimationInstance.__init__(animation_id, animation_raw_display_name, animation_type)`：
- `self.display_name = animation_raw_display_name`（我们改的就是这里 → 中文）；
- `self.h = hash_string("<前缀>." + str(animation_id))`；
- `self.localized = TurboLocalizedString(self.h, animation_raw_display_name)` —— **以 hash key 为主体、以 raw 文本为 fallback**。

若挑选器 UI 渲染 `self.localized`（TurboLocalizedString），则**游戏会先按 hash key 查本地化表**，查不到才回退 raw。因此「最终显示了什么」不完全由我们覆盖的字段决定，而由**那行 UI 到底调用 `display_name` 还是 `localized`** 决定——而这只能在**真机读 WW 的 .pyc** 判定（ECS 无 WW 包）。

---

## 1. 三假设与一句话判别（先在脑子里锁定方向）

| # | 假设 | 一句话判别 |
|---|---|---|
| A | WW **不走**「Mods loose override 可遮蔽」的资源路由（XML 在它自己的 ts4script/别处被打包或按变体 key 读） | 无论缓存/优先级怎么弄，override 都不会被读到 → 本包路径不适用于该类型，需另想覆盖面（见 §5 结论） |
| B | override 在 Mods 存在但**没被加载**（缓存/层深/重名/**Mods 里还有第二个 WW 目录**更高优先级） | 清缓存/移浅层/查重后能读到 |
| C | XML 被读到了、`display_name` 确实变中文，但 **UI 渲染的是 `localized`（hash key）**，而该 key 无 STBL（我们按约束不建）→ 回退英文 | 需读 WW .pyc / 或做一个「旁证」看 UI 实际取哪个属性 |

**判别顺序：先低成本排 B，再用「决定性测试」锁定 A vs C。**

---

## 2. 第一轮：排 B（override 到底被游戏加载没有）——全部只读

### 2.1 确认 override 真的在游戏会扫的目录里
- 你放的文件完整路径是什么？确认扩展名是 `.package`、在 `Mods\`（含任意子目录，但**确认层级不过深、文件名不含非 ASCII/特殊字符**）。
- 有没有可能文件名是 `P35.1_ww_anim_displayname_override.package` 但被安全软件/拖放改名（如 `.package.txt`）？查一下扩展名。

### 2.2 扫「Mods 内是否还有第二个来源也 carry 同一 instance」
某处若另有一个包（含 WW 本体残留、或又一个动画 override、或旧资源目录子副本）也含 `0x7DF2169C / 0x43F3438A94EDEB2B`，游戏按优先级取**其中一个**，可能是那份（英文）胜出。
只读扫描（Windows PowerShell，仅列出，不改）：
```powershell
Get-ChildItem "$env:USERPROFILE\Documents\Electronic Arts\The Sims 4\Mods" -Recurse -Filter *.package |
  Where-Object { $_.Length -gt 0 } | ForEach-Object {
    $hits = Select-String -Path $_.FullName -Pattern ([byte[]](0x9C,0x16,0xDF,0x7D)) -Encoding Byte -SimpleMatch -Quiet
    if ($hits) { $_.FullName + "  <-- contains type 0x7DF2169C bytes" }
  }
```
> 这是按**类型四字节**粗扫（DBPF index/body 里 type id 以小端出现，bytes `9C 16 DF 7D`）。命中后只读打开该包看其 group/instance 是否也= `0 / 0x43F3438A94EDEB2B`。只要 Mods 里**同 instance 的包不止一个** → 优先级冲突成立（先记为 B/冲突，做 §2.4 二分）。

### 2.3 清缓存（只读? 否——但这是游戏标准操作，允许；不写源/不建 STBL）
Sims 资源缓存会遮蔽新包。标准做法（会删缓存文件，属游戏自产缓存，不在「不修改 package/不建 STBL」红线内）：
- 退出游戏。
- 备份并删除：`The Sims 4\localthumbcache.package`（可整个删，游戏重建）。
- 若仍英文，再删一次并同时删 `The Sims 4\GameVersion.txt`? 不需要——只需 localthumbcache。
- 重启验证。

### 2.4 优先级/重名二分（只读定位，不改任何包内容）
若 §2.2 发现 ≥2 个包同 instance：把**除 P35 override 外**的其它同 instance 包**逐个临时移出**到备份目录（**移，不改内容，不删**），每次只移一个→清缓存→进游戏看**是否这批 479 变中文**。二分出「是谁抢了优先级」，再把拿掉的放回（决定取舍由你定，我们不动 WW 源）。

### 2.5 若 2.1–2.4 全干净仍英文 → 进入 A/C 判别，**停**在这里，别再反复删缓存。

---

## 3. 决定性测试：A vs C（这个测试能一刀切开）

先在 Mods 里**临时把那 48 条 KEEP==英文 之外的某条最强中文**（如 ordinal 里 `CHANGED` 的一条，中文最醒目）记下 animation 名。

**测试思路**：同一 XML override 里，我们把**整段 display_name 字段**都改成中文了。若游戏**确实读到了我们的 override**（B 已排除），那它拿到的 `animation_raw_display_name` 就是中文——此时无论它走 `display_name` 还是 `localized.fallback`，最终英文都讲不通（除非 `localized` 的 hash key 命中了**WW 自带的英文 STBL/string**，见下）。

> 关键：**WW 在选择器 UI 可能用 `localized`（hash key），而该 hash 命中 WW 自己带的英文本地化串**（不是我们该建的 STBL），于是回显英文原文 —— 这就是最常见的「字段改了但 UI 仍英文」根因，且**不建 STBL 恰恰没法治本**（约束内则此为「范围外已知项」，见 §5）。

**要锁定，需真机读 WW 的 UI 消费代码**（只读反汇编，ECS 没有 WW 包）：
用项目既有工具枚举/反汇编 WW 脚本包里的挑选器 —— 看渲染 display 名那行属性：
```powershell
# 定位 WW 动画相关 .pyc 成员（只读）
# (WW 脚本 ts4script 路径视你安装而定，例)
$ww = "C:\...\Mods\WickedWhimsMod\TURBODRIVER_WickedWhims_Scripts.ts4script"
& python scripts\ww_p29a_game_py.py magic-from-pyc --locate-mod "C:\...\Mods"
```
然后在反汇编文本中找**选择/播放动画时用的是 `localized` 还是 `display_name`**。若命中本地化 key 逻辑 → 判定 C。

---

## 4. 判断树汇总（照此回报，我据此定案）

你在跑完 §2 后回报以下任一**分支结果**：
- **[果B-无冲突]**：Mods 里只有这一个同 instance 包；已清缓存；仍英文 → 疑 A 或 C。
- **[果B-冲突]**：找到第 2 个同 instance 包，名字/来源列出 → 先做 §2.4 二分。
- **[果A/C 判别输入]**：
  a) 把 override 移出 Mods → 英文照旧（预期）。这不证明任何事，只是基线。
  b) 用只读反汇编（§3）确认 UI 取 `localized`（hash）还是 `display_name`。
  c) （可选旁证）临时只把**某 1 条**任意改成一段**不可能出现在 WW 任何英文串里**的标记串（如 `ZZZ_TEST_只读旁证`）→ 若 UI 出现该标记 → 证明读到 override 且直显 raw → 反推当前英文是「UI走 hash/localized 的 WW 英文」→ C 坐实；若仍英文原样 → 证明压根没读到 → A 坐实。
     > 注：c) 是「临时旁证包」，会多写一个 .package 到 Mods；用完即移走删除，属于排查用临时件，不触碰源、不生成 STBL、不重译。是否允许执行该旁证由你决定（默认不做，先听你指示）。

---

## 5. 三种定案的后续（都不在 P35.4 现场改包）

- **B（没加载）**：按 §2 修正目录/缓存/冲突 → 重新验证即可，无需改任何内容。
- **C（UI 走 WW 自带英文本地化 key）**：
  - 约束红线「不生成 STBL」下，这属**范围外已知项**：我们的 XML override 让 `display_name` 成中文，但 WW 挑选器 UI 用自带的英文 hash-string 显示。
  - 要真正让 UI 中文化，需覆盖的是**那部分 WW 自带字符串资源**（可能需 WW 侧扩展/另行授权的覆盖方式）——超出 P35 当前工件能力。**记录为范围外，不扩容本包。**
  - 若你希望，我可以另给出「该消费点具体资源/主键」的只读反汇编定位脚本案（仍不建 STBL、不写 Mods）。
- **A（不走 loose override 路由）**：
  - WW_ANIM_XML 类型可能不被 Sims 以普通 XML override 方式遮蔽（或 WW 以变体 flags 读 key）。
  - P35 工件本身自洽正确（P35.2 PASS），只是**投递方式不适用于该类型**。需评估替代投递（例如 WW 约定/脚本注入），属新决策，**等你批准再进入**，本阶段不做。

---

## 6. 请回报的最小信息集
1. override 在 Mods 的完整路径 + 扩展名确认。
2. §2.2 扫描结果：Mods 内同 `0x7DF2169C`（尤其同 instance）的包数量与路径。
3. 是否已删 localthumbcache 后仍英文（yes/no）。
4. 你能否执行 §3 的只读反汇编判别，或同意 §4-c 的临时旁证包（默认不动）。

收到后我据以收敛到 A/B/C 之一并给出**定案结论 + 一个可批准的下步动作**（仍不建 STBL、不写 WW 源）。
