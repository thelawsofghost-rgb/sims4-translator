# P35.4 — 游戏内验证手册（WickedWhims 479 display_name override）

> 阶段性质：**只读验证 + 故障排查**。ECS/小龙虾**不执行任何复制**。
> 不修改 package / 不生成 STBL / 不重新翻译。
> 约束不变：`ZERO_WRITE_TO_MODS=YES` `ZERO_WRITE_TO_SAVES=YES`。
> 最终工件：`output/p35/P35.1_ww_anim_displayname_override.package`（已由用户在本地放入 Mods）。

---

## 0. 部署物料快照（P35.1/P35.2 结论，作为第 1 步基线核对）

| 项 | 值 | 来源 |
|---|---|---|
| 类型 type_id | `0x7DF2169C`（WW_ANIM_XML） | P35.2 VERDICT=PASS |
| 真实 instance | `0x43F3438A94EDEB2B` | P35.2 |
| WW_ANIM_XML 唯一性 | 1（UNIQUE=OK） | P35.2 |
| ordinal 覆盖 | 479 / 479（ENTRY_COUNT=OK） | P35.2 |
| OVERRIDE_COUNT | 479 | P35.2 |
| 变化数 CHANGED_COUNT | 431 | P35.2 |
| 保留数 KEEP_COUNT | 48 | P35.2（等于英文 raw =合法） |
| VERDICT | PASS | P35.2 |

> 注意：KEEP_COUNT=48 表示有 48 条 final display_name **等于源英文**。这**不是缺陷**（P35 语义允许保留英文）；游戏验证时这 48 条应显示英文原文，勿当异常。

---

## 1. 验证 checklist（逐项打勾）

### A. 启动与稳定性
- [ ] A1 游戏能正常启动，无启动崩溃、无卡在加载界面。
- [ ] A2 游戏内「脚本调用/通知」无红色错误弹窗直接报 WW_ANIM 相关异常。

### B. WickedWhims 功能完整性（回归）
- [ ] B1 WW 主菜单 / 动画数据库页面可正常打开。
- [ ] B2 动画可筛选、可排序、可预览（预览缩略图/动画本体加载正常）。
- [ ] B3 播放/切换动画无异常（动画文件本身未受影响——本次只覆盖 display_name 字段）。
- [ ] B4 其它 WW 功能无回归（设置页、Sex 流程入口能进）。

### C. 479 display_name 加载（**核心验收**）
- [ ] C1 动画列表/挑选器中，display_name 显示为**中文**。
- [ ] C2 抽样核对：中文条 ≠ 源英文翻译错误；抽查含特殊字符条目能正确显示（`&` `<` `>` 等已字节级保真，见 P35.1 反向替换证明）。
- [ ] C3 那 48 条 KEEP 条目显示为英文原文（合法，不视为失败）。
- [ ] C4 无中文条目显示成乱码、方块、`#HashName!` 或空名（空名=异常信号，见 §2.4/§2.5）。
- [ ] C5 覆盖后无「英文显示名被污染成半中半英拼接」类错位（若某条只改了一半=字段错位信号，见 §2.6）。

---

## 2. 故障排查方案（定位链路）

**数据优先级**：lastException.txt > WickedWhims 运行时日志 > 游戏内显示 > 冲突排查。
按此顺序取证据，别先改包。

### 2.1 先取三大日志（只读）
1. **lastException.txt**：`模拟人生目录\The Sims 4\lastException.txt`（或 game/LastException）。
   - 用文本编辑器打开，重点看：
     - 有无 `WW_ANIM` / `0x7DF2169C` / `43F3438A94EDEB2B` / `animations_list` / `animation_raw_display_name` 字样。
     - 第一条 SSE（Stack/会话异常）的**模块**和**时间戳**——时间与放入 Mods 后首次启动是否吻合。
     - 是否 `Resource` / `override` / `load` / `parse` 类异常（XML override 损坏信号）。
2. **WickedWhims 日志**：`The Sims 4\WickedWhims Logs` 或文档内 WW 自写日志。
   - 搜 `display_name`、`Animator`、`(WW_ANIM_XML|7DF2169C)`、`failed`、`exception`。
3. **游戏内显示**：截图/描述——是「整体不显示中文」还是「个别条目异常」（两种方向不同，见 §2.3 vs §2.5）。

### 2.2 判定矩阵：启动/功能崩溃
| 现象 | 可能根因 | 下一步 |
|---|---|---|
| 启动即崩、进 WW 即崩 | override TGI 冲突 / XML 被游戏拒载 / 旧缓存放了坏包 | §2.3 + §2.4 |
| 能进游戏但 WW 界面打不开 | UI 表 / 本地化串冲突（非本包） | §2.5，核对 lastException 的 instance |
| 动画能播但名字没变 | override 没被读到（缓存/优先级） | §2.4 |

### 2.3 XML override 冲突排查（**最可能出现的问题**）
- 若另一个 Mod（含 WW 本体残留、其它动画 override、xlm 改中文的老包）也写了**同一 TGI**（`0x7DF2169C / 0x43F3438A94EDEB2B`），游戏按**资源优先级**只加载一个 → 可能你的 479 没生效，或别人在生效。
- 排查：在 Mods 里**全局搜**是否还有第二个含该 instance 的 `.package`：
  - 用 S4PE / Sims4Studio 或 `grep -l`（Windows 可直接用文本工具/资源查看器）扫描 `Mods\**\*.package`，抽取每包的 index 看是否出现 `WW_ANIM_XML + 0x43F3438A94EDEB2B`。
  - 冲突发现后：**重命名/移走对方那个覆盖包**（而非改源），再进游戏验证；确认 P35 生效后决定取舍。
- 原则：**绝不动 WW 原包**，冲突侧用「临时移出」二分定位，不用修改。

### 2.4 读不到 override（启动但英文不变 / 全英文）
排查优先级：
1. **缓存**：删 `The Sims 4\localthumbcache.package` 与首启后重建的 resource 缓存（先备份再做），重进游戏。
2. **文件名/路径层深**：Mods 子目录过深或文件名含特殊字符导致资源未被索引 → 移到 `Mods\` 浅层、保持 `.package` 后缀。
3. **游戏版本不认该 loose override 包**：若 WW 动画 XML 需经特定加载器而非普通 loose override，则本 loose 包方式不生效——核对 lastException 是否 `Resource not found`；此时回滚方案见 §2.6。
4. 用 §2.5 的独立旁证确认「游戏到底读到哪套 display_name」。

### 2.5 显示含 `#HashName!` / 空名 / 乱码
- `#HashName!` 或空名 = 游戏在**按其内部 string-table 解析** display_name 而非直接显示原文 → 说明该字段被当作 key 去查 STBL，找不到就显示裸 hash/空。
  - 这与「P35 直接覆盖字段文本」期望不符时，说明该处消费逻辑不走直接文本。
  - 处理：**不要生成 STBL**（P35 硬约束）。此现象属「消费端要求 STBL key」，属范围外——记录并回退该显示点，不硬改。
- **乱码/方块**（非 #hash）：编码问题信号——回到 P35.1 的 raw-encoding 契约核对某一条的字节，但**不修改已部署包**，先确认为个别还是全量。
- 中文条目拼到一半错位（半中半英），见 §2.6。

### 2.6 「动一半」或个别字段错位（非预期）
- 若出现某条前半中文后半英文残句、或相邻两条互串：
  - 疑 P35.1 在该条目上的**字段定位/边界**问题。不要现场改包。
  - 回滚到该处的证据路径：用 P35.0 对齐工具只读对比该 ordinal 在 source vs override 的 raw（不改、仅 dump），确认替换是否精确；确认后需**重新生成 override + 重跑 P35.2 + 重新部署**（下一轮，不在 P35.4 现场改）。

### 2.7 回滚预案（若必须止血）
- 因 P35.2 已证明工件自洽，绝大多数问题属「部署/冲突/消费方式」，优先：
  1. 移出冲突包（§2.3）→ 清缓存（§2.4）→ 重验证。
  2. 仍异常才考虑**移除** `P35.1_ww_anim_displayname_override.package` 出 Mods，恢复英文原状（不影响 WW 本体），然后带着 lastException 回来定位。
- 整个过程**不改源 WW 包、不建 STBL**。

---

## 3. 判定标准（何时算 PASS）
- A/B 全部 ✅ 且：
  - C1 中文条正确显示中文；
  - C4 无 `#HashName!`/空名针对性触发（若为 STBL-key 消费端的既有限制则记为**范围外已知项**而非 P35 defect）；
  - lastException 无与 `0x43F3438A94EDEB2B` 相关的 parse/resource 异常；
  - 无 XML override 冲突，或冲突已二分排出。
- 关键指标建议记录：中文显示覆盖可肉眼统计（≥期望比例）、CHANGED=431 里实际可见中文条数、KEEP=48 保持英文条数。

## 4. 产物说明（本阶段不产出新工件）
- 本文档即 P35.4 checklist/排查入口。若需更深度自动取证（lastException 关键词扫描脚本），可要求另建只读分析脚本，仍不写 Mods/不生成 STBL。
